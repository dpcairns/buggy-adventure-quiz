// get user data from form on submit and store it in local storage

// render metadata to page
//      quest detail page (quest)
//      user profile and header     
//      quest list page (map)
//      results page pulls from metadata (use state to decide what results to show from metadata)

// render state to page
//      completed quests
//      user profile
//      results

// write the metadata (data modeling)


//requirements

    //choice, dynamically rendered from set state and query params
    // run out of choices - show results
    // track state of user based on choices